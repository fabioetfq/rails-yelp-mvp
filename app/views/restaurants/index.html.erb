<h1>All Restaurants</h1>
<br>
<%= link_to "Create a new restaurant", new_restaurant_path, class: "btn btn-secundary" %>
<br>
<div>
<% @restaurants.each do |item| %>
  <h2><%= link_to item.name, restaurant_path(item) %></h2>
  <p>Category: <%= item.category.capitalize %></p>
<% end %>